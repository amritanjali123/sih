"use strict";
const router = require("express").Router();

// router.get("/image/:name", (req, res) => {
//   res.sendFile(__dirname + "/../../../res/img/" + req.params.name);
// });

module.exports = router;
